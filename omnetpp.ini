[General]
network = server_client.MultiServerNetwork
sim-time-limit = 100s

# Configure client behavior
*.client[*].sendInterval = 1s      # Each client sends a request every second

# Configure server behavior
*.server[*].processingTime = 0.5s  # Each server takes 0.5 seconds to process each request
